# Frontend (Dev)

This is a minimal Vite + React scaffold to interact with the backend dev API.

Run locally:

```bash
cd frontend
npm install
npm run dev
```

The backend dev server should be running at `http://localhost:3000` (see `backend/index.js`).
